<h1>Category Show Page</h1>


<ul>
  <% @category.items.each do |item| %>
    <p>  <%= item.name %> </p>
    <p>  <%= number_to_currency(item.price) %> </p>
    <%= image_tag item.image_path, class: "itemimage", size: "400x300" %>
  <% end  %>
</ul>
